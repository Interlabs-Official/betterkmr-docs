import{_ as e,c as n,o as s,ae as t}from"./chunks/framework.ixH79I-5.js";const u=JSON.parse('{"title":"🔧 Mastering JavahnScript (Concept)","description":"best thing ever","frontmatter":{"title":"🔧 Mastering JavahnScript (Concept)","description":"best thing ever"},"headers":[],"relativePath":"develop/javahnscript.md","filePath":"develop/javahnscript.md"}'),i={name:"develop/javahnscript.md"};function p(o,a,l,c,r,h){return s(),n("div",null,a[0]||(a[0]=[t(`<h1 id="🔧-mastering-javahnscript-concept" tabindex="-1">🔧 Mastering JavahnScript (Concept) <a class="header-anchor" href="#🔧-mastering-javahnscript-concept" aria-label="Permalink to &quot;🔧 Mastering JavahnScript (Concept)&quot;">​</a></h1><p>This guide assumes you know Python as a base point.</p><p>Learning JavahnScript is easy. From a beginner&#39;s perspective, it can look challenging compared to other languages. However, mastering JavahnScript is the fun part.</p><p>Take this example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>message = javahn_input: command(text);save=&quot;Hello from .jvhn file!&quot;</span></span>
<span class="line"><span>javahn_print: Hi!</span></span>
<span class="line"><span>counter = javahn_input: command(number);save=&quot;42&quot;</span></span>
<span class="line"><span>javahn_if: counter == &quot;43&quot;</span></span>
<span class="line"><span>javahn_print: It is 43 instead actually!</span></span></code></pre></div><p>Let&#39;s break it down into chunks.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>message = javahn_input: command(text);save=&quot;Hello from .jvhn file!&quot;</span></span></code></pre></div><p>First of all, we have the <code>message</code> variable. However, unlike other languages like Python, we have to define what we&#39;re going to be storing in this variable, hence <code>javahn_input</code>. In the future, this would be good to prevent overwriting a variable with something that doesn&#39;t match its type.</p><p>Again, we also have to define something else. This is technically the actual type. <code>command(text)</code> tells the interpreter that what we are going to be storing in this variable will be a string. Make sure this is followed by a semicolon (<code>;</code>).</p><p>Next, we have <code>save=&quot;Hello from .jvhn file!&quot;</code>. After the semicolon, we now specify what we want to write to that variable. Since we&#39;ve chosen &quot;text&quot;, we can now input our string, surrounded by double quotes.</p><p>Now, to print a message, we can use <code>javahn_print</code>. In this example, we&#39;ve specified a variable, although below will also work:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>javahn_print: Hi!</span></span></code></pre></div><p>The reason this works is because <code>javahn_print</code> will always expect the input to be a string. Therefore, we do not need to surround our string with quotes when we use <code>javahn_print</code>.</p><p>Let&#39;s take this a step further and utilise numbers in a variable.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>counter = javahn_input: command(number);save=&quot;42&quot;</span></span></code></pre></div><p>Same principle as before, however instead of <code>command(text)</code>, we use <code>command(number)</code> for extra high-fidelity (as we want the interpreter to know what we&#39;re storing). However, the number itself should still be stored in a string.</p><p>If statements! We love these. However, in JavahnScript, since we favour simplicity over functionality, if statements are now in the same line and nothing needs to be indented.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>javahn_if: counter == &quot;42&quot;</span></span></code></pre></div><p>We&#39;re using the counter variable we defined, and since JavahnScript loves strings, we are comparing that number variable to a string. Crazy!</p><p>Then, for good practice, we shall end the script to prevent possible memory leaks because, as stated previously, we prefer simplicity over functionality.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>javahn_script_thread::END()</span></span></code></pre></div><p>To run our script that we&#39;ve made, save it in a file that ends in .jvhn in the same directory as JavahnScript.py, and specify it when you run it in a terminal/command prompt.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&gt; python JavahnScript.py example.jvhn</span></span>
<span class="line"><span>Hi!</span></span>
<span class="line"><span>Condition is true!</span></span></code></pre></div><p>If you want, you can make things go more in depth (there are some new things, but mostly the same e.g. <code>javahn_endif</code>):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>message = javahn_input: command(text);save=&quot;Hello from .jvhn file!&quot;</span></span>
<span class="line"><span>javahn_print: message</span></span>
<span class="line"><span>counter = javahn_input: command(number);save=&quot;42&quot;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>javahn_if: counter == &quot;42&quot;</span></span>
<span class="line"><span>javahn_print: The counter is 42!</span></span>
<span class="line"><span>message = javahn_input: command(text);save=&quot;This will only show if counter is 42&quot;</span></span>
<span class="line"><span>javahn_print: message</span></span>
<span class="line"><span>javahn_endif:</span></span>
<span class="line"><span></span></span>
<span class="line"><span>javahn_if: counter == &quot;43&quot;</span></span>
<span class="line"><span>javahn_print: This won&#39;t show because counter is not 43</span></span>
<span class="line"><span>message = javahn_input: command(text);save=&quot;This won&#39;t be saved&quot;</span></span>
<span class="line"><span>javahn_print: This won&#39;t show either</span></span>
<span class="line"><span>javahn_endif:</span></span></code></pre></div><p>Which outputs:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&gt; python JavahnScript.py example.jvhn</span></span>
<span class="line"><span>Hello from .jvhn file!</span></span>
<span class="line"><span>The counter is 42!</span></span>
<span class="line"><span>This will only show if counter is 42</span></span></code></pre></div>`,27)]))}const v=e(i,[["render",p]]);export{u as __pageData,v as default};
